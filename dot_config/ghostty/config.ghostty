scrollback-limit = 500000000
theme = Aurora
font-family = "MesloLGS NF"

# When SSHing into a Linux host lacking the ghostty terminfo entry, run
# infocmp -x xterm-ghostty | ssh <host> -- tic -x -
# https://ghostty.org/docs/help/terminfo#ssh
# Packages to install on Debian/Ubuntu (apt) and Fedora (dnf).
# One package name per line. Lines starting with # are ignored.
# Note: on Arch Linux the package is `go`, not `golang` — see packages_arch.txt.
ffmpeg
golang
